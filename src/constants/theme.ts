/**
 * Global color theme. Import once (e.g. in main.tsx) then use var(--color-name) in any stylesheet.
 *
 * W  = white
 * B  = black
 * DP = dark pink
 * MP = medium pink
 * LP = light pink
 */

export const Colors = {
  white: "#ffffff",
  black: "#000000",
  darkPink: "#ff7aad",
  mediumPink: "#ff9bc5",
  lightPink: "#ffcfe3",
};
